<h2>Listing tasks</h2>

<div id='tasks' data-update-url="<%= sort_tasks_url %>">
  <%= render @tasks %>
</div>

<%= form_for(Task.new, remote: true) do |f| %>
  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
