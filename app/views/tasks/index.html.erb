<h2>Listing tasks</h2>

<div id='tasks'>
  <%= render @tasks %>
</div>

<%= form_for(Task.new, remote: true) do |f| %>
  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
